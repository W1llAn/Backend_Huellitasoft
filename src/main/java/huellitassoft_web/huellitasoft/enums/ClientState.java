package huellitassoft_web.huellitasoft.enums;

/**
 * Enum que define los estados posibles de un cliente.
 * 
 * ACTIVO: Cliente en estado activo, puede utilizar los servicios
 * INACTIVO: Cliente en estado inactivo, no puede utilizar los servicios
 */
public enum ClientState {
    ACTIVO,
    INACTIVO,
    BLOQUEADO
}
